{
	"layer": "top",
	"position": "top",
	"height": 30,
	"spacing": 16,
	"modules-left": ["sway/workspaces", "sway/mode"],
	"modules-center": ["clock", "custom/weather"],
	"modules-right": ["idle_inhibitor", "custom/kernel", "network", "cpu", "memory", "temperature", "custom/audio", "battery", "tray"],
	"battery": {
		"states": {
			"good": 95,
			"warning": 30,
			"critical": 15
		},
		"format": "{icon} {capacity}%",
		"format-charging": " {capacity}%",
		"format-plugged": " {capacity}%",
		"format-alt": "{icon} {time}",
		"format-good": "",
		"format-full": "",
		"format-icons": ["", "", "", "", ""]
	},
	"clock": {
		"format": "{:%a %b %e %I:%M %p}",
		"format-alt": "{:%c}",
		"interval": 1,
        	"tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
	},
	"cpu": {
		"format": " {usage}%",
        	"tooltip": false
	},
	"custom/audio": {
		"format": "{}",
		"exec": "$HOME/.config/waybar/scripts/audio.sh",
		"on-click": "audioctl toggle-mute-output",
		"interval": 1
	},
	"custom/kernel": {
		"format": " {}",
		"exec": "uname -r",
		"interval": 600
	},
	"custom/weather": {
		"format": " {}",
		"exec": "curl 'https://wttr.in/?format=1' | cut -d ' ' -f 3 | sed 's/+//'",
		"interval": 3600
	},
	"idle_inhibitor": {
		"format": "{icon}",
		"format-icons": {
			"activated": "",
			"deactivated": ""
		}
	},
	"memory": {
		"format": " {}%"
	},
	"network": {
		"format-wifi": " {essid} ({signalStrength}%)",
		"format-ethernet": " {ipaddr}",
		"tooltip-format": "{ifname} via {gwaddr}",
		"format-linked": " {ifname} is offline",
		"format-disconnected": "⚠ Disconnected",
		"format-alt": "{ifname}: {ipaddr}/{cidr}"
	},
	"sway/mode": {
		"format": "<span style=\"italic\">{}</span>"
	},
	"temperature": {
		"critical-threshold": 70,
		"format-critical": "{icon} {temperatureC}°C (CRITICAL)",
		"format": "{icon} {temperatureC}°C",
		"format-icons": ["", "", ""]
	},
	"tray": {
        	"icon-size": 16,
		"spacing": 12
	},
}


#!/bin/sh

# musicpls - Simple music downloader using yt-dlp

echo -n "Please select a playlist or upload: " && read url; yt-dlp -x --audio-format 'mp3' -o '%(playlist_index)s - %(title)s.%(ext)s' "$url"

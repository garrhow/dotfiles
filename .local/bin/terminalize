#!/bin/sh

# Terminalize - X display checker

### If the user is currently in an X session, the program will execute $1, otherwise, it will execute $2

if [ $DISPLAY ]; then
	exec $1;
else
	exec $2;
fi

#!/bin/sh

chosen=$(cut -d ';' -f1 $HOME/.local/share/emoji | dmenu -i -l 10 | sed "s/ .*//")

[ -z "$chosen" ] && exit

if [ -n "$1" ]; then
	xdotool type "$chosen"
else
	printf "$chosen" | xclip -selection clipboard
fi
